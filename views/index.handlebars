<br>
<h1 class="devoured"> <strong> Eat DA-Burger App </strong></h1>
<br>

<h2 class="devoured">Burgers that are not Devoured</h2>
<br>

<ul class="devoured1">
  {{#each burgers}}
    {{#unless sleepy}}
      {{> burger/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2 class="devoured">Burgers That are Devoured!</h2>
<br>
<ul class="devoured2">
  {{#each burgers}}
    {{#if sleepy}}
      {{> burger/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>
<br>

{{!-- add a burger  --}}
<h2 class="form-group2">Add a Burger</h2>
<br>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleep">Devoured?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devoured<br>
    <input type="radio" name="sleepy" value="0"> NOT Devoured
  
  <br>
   <button type="sumbit" class="btn btn-primary ">Add Burger</button>
  </div>
</form>